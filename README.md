# HS Contact Property Value Query
CLI to pull property value of a contact 

## Super Simple Steps
* clone the repo
* npm install && npm start

When prompted, enter the internal name of the property value you want.